services:
  space:
    build:
      context: .
      dockerfile: ./docker/Dockerfile
      target: prod
      args:
        VITE_API_URL: ${VITE_API_URL}
    ports:
      - "80:80"
    restart: unless-stopped
