services:
  space:
    build:
      context: .
      dockerfile: ./docker/Dockerfile
      target: dev
    volumes:
      - .:/app
      - node_modules:/app/node_modules
    ports:
      - "5173:5173"
    env_file:
      - .env.development

volumes:
  node_modules:
