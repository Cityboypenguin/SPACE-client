services:
  space: # ここはフロントのサービス名にする
    build:
      context: .
      dockerfile: ./docker/Dockerfile
    volumes:
      - .:/app
      - node_modules:/app/node_modules
    ports:
      - "5173:5173"
    command: npm run dev

volumes:
  node_modules:
